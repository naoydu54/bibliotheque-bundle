<link rel="stylesheet" type="text/css" href="{{ asset("#{prefix}/plugins/bootstrap-dialog/css/bootstrap-dialog.min.css") }}">
<link rel="stylesheet" type="text/css" href="{{ asset("#{prefix}/plugins/context-menu/jquery.contextMenu.css") }}">
<link rel="stylesheet" type="text/css" href="{{ asset("#{prefix}/plugins/bibliotheque/jquery.bibliotheque.css") }}">
<style>
    .img-preview{
        max-width: 201px;
        border-bottom-left-radius: 0;
        border-bottom-right-radius: 0;
    }

    .bbl-inner{
        border-bottom: 2px solid #e1e1e1;
    }
    .bbl-wrapper{
        border: 0;
        background-color: white;
    }

    #tree{
        border-right: 5px solid #e1e1e1;
    }

    .ip_bibliotheque_action{
        margin-bottom: 10px;
    }

    .ip_bbl_btn-group > .btn:first-child:not(:last-child):not(.dropdown-toggle){
        border-top-left-radius: 0;
    }

    .ip_bbl_btn-group > .btn:last-child:not(:first-child){
        border-top-right-radius: 0;
    }

    /* The Modal (background) */
    .popup {
        display: none; /* Hidden by default */
        position: fixed; /* Stay in place */
        z-index: 2; /* Sit on top */
        left: 0;
        top: 0;
        width: 100%; /* Full width */
        height: 100%; /* Full height */
        overflow: auto; /* Enable scroll if needed */
        background-color: rgb(0,0,0); /* Fallback color */
        background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
        -webkit-animation-name: fadeIn; /* Fade in the background */
        -webkit-animation-duration: 0.4s;
        animation-name: fadeIn;
        animation-duration: 0.4s
    }

    /* Modal Content */
    .popup-content {
        position: fixed;
        bottom: 0;
        background-color: #fefefe;
        border: 1px solid #888;
        width: 100%;
        -webkit-animation-name: slideIn;
        -webkit-animation-duration: 0.4s;
        animation-name: slideIn;
        animation-duration: 0.4s
    }

    .popup .close-popup{
        border-radius: 0 !important;
        font-size: 20px;
        padding: 2px 11px;
    }

    /* The Close Button */
    /*.close-popup {
        position: absolute;
        right: 0;
        color: #aaaaaa;
        float: right;
        font-size: 28px;
        font-weight: bold;
        margin-right: 10px;
    }*/
</style>